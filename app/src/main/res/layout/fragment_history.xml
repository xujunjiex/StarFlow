<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface_variant"
    android:orientation="vertical">

    <!-- 顶部栏：Tab + 清空按钮 -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="8dp"
        android:paddingTop="8dp"
        android:paddingBottom="4dp">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/history_tab_layout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            app:tabMode="fixed"
            app:tabGravity="start"
            app:tabIndicatorColor="#55AEEA"
            app:tabSelectedTextColor="#333333"
            app:tabTextColor="#888888" />

        <ImageButton
            android:id="@+id/btn_refresh_history"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_popup_sync"
            android:tint="@color/primary"
            android:contentDescription="@string/refresh_history" />

        <ImageButton
            android:id="@+id/btn_clear_all_cache"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/bin_clean"
            android:tint="#FF5722"
            android:contentDescription="Clear All Cache" />

        <ImageButton
            android:id="@+id/btn_history_settings"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_manage"
            android:tint="@color/text_secondary"
            android:contentDescription="Settings" />
    </LinearLayout>

    <!-- 视图模式 Tab 和引擎选择器 -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/viewModeTabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:colorBackground"
        app:tabMode="fixed"
        app:tabGravity="fill" />

    <LinearLayout
        android:id="@+id/engineSelectorLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="12dp"
        android:paddingVertical="6dp"
        android:background="@color/surface_secondary"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/history_retranslate_engine_label"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:layout_marginEnd="8dp" />

        <AutoCompleteTextView
            android:id="@+id/spinnerOcrEngine"
            android:layout_width="0dp"
            android:layout_height="36dp"
            android:layout_weight="1"
            android:inputType="none"
            android:textColor="@color/text_primary"
            android:textSize="13sp"
            android:background="@drawable/custom_radio_button_background"
            android:paddingHorizontal="12dp"
            android:completionThreshold="0" />
    </LinearLayout>

    <!-- 内容区域 -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- 游戏历史列表 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_game_history"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:clipToPadding="false" />

        <!-- 漫画历史列表 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_manga_history"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:clipToPadding="false"
            android:visibility="gone" />

        <!-- 空状态 -->
        <TextView
            android:id="@+id/tv_empty_state"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="@string/no_history"
            android:textColor="@color/text_tertiary"
            android:textSize="13sp"
            android:visibility="gone" />
    </FrameLayout>

</LinearLayout>
