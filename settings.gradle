pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "StarFlow"
include ':app'
// 本地 GGUF 推理（llama.cpp 源码自编）—— 独立模块的原因见 llamacpp/src/main/cpp/CMakeLists.txt
include ':llamacpp'